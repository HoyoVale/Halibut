#include "halibutpch.h"
#include "HALIBUTResourceAllocator.h"

namespace HALIBUT
{
    HALIBUTResourceAllocator::HALIBUTResourceAllocator(HALIBUTDevice& device)
        : m_Device(device)
    {
    }
    HALIBUTResourceAllocator::~HALIBUTResourceAllocator()
    {
    }
}